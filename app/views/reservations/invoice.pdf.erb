
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Invoice</title>
    <style>
     @page {
      size: A5;
      margin: 15mm 10mm 15mm 10mm;
    }
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
        background: #f8f9fa;
      }
      body {
        font-family: 'Arial', sans-serif;
        color: #3B3B1A;
        box-sizing: border-box;
      }
      .invoice-container {
        background: #fff;
        width: 100%;
        max-width: 180mm;
        min-height: 257mm;
        margin: 0 auto;
        padding: 24mm 18mm;
        border-radius: 16px;
        box-shadow: 0 4px 24px rgba(0,0,0,0.08);
        box-sizing: border-box;
      }
      h1 {
        color: #AEC8A4;
        font-size: 2.5rem;
        margin-bottom: 2rem;
        text-align: center;
      }
      p {
        font-size: 1.3rem;
        margin: 0.7rem 0 1.2rem 0;
      }
      .label {
        color: #7a8c5c;
        font-weight: bold;
      }
      .total {
        font-size: 1.5rem;
        color: #3B3B1A;
        font-weight: bold;
        margin-top: 2.5rem;
      }
    </style>
  </head>
  <body>
    <div class="invoice-container">
      <h1>Invoice for Reservation <%= @reservation.id %></h1>
      <p><span class="label">Customer:</span> <%= @reservation.user.first_name %></p>
      <p><span class="label">Pet:</span> <%= @reservation.pet.name %></p>
      <p><span class="label">From:</span> <%= @reservation.start_date.strftime("%-d. %B %Y") %></p>
      <p><span class="label">Until:</span> <%= @reservation.end_date.strftime("%-d. %B %Y") %></p>
      <p class="total">Total: $<%= @reservation.pet.price %></p>
    </div>
  </body>
</html>
